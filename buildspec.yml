languages:
  - phpunit

variables:
  - key: value

phases:
  pre:
    - composer install

  test:
    - phpunit

  build:
    - echo "Build phase"

  post:
    - echo "Post phase"

  deploy:
    - echo "Deploy phase"
